# Frontend environment variables for Vite (development)
# Copy this file to `.env` in the same folder and adjust values if needed.

# Use the Vite dev proxy so the frontend calls `/api/...` which is proxied to Spring Boot on :8080
VITE_API_BASE=/api

# Cesium Ion token used by the CesiumJS viewer in the browser
# Get your token at https://ion.cesium.com/tokens
VITE_CESIUM_ION_TOKEN=REPLACE_WITH_YOUR_TOKEN
