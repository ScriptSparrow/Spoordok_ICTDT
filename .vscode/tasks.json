

{

    "tasks": 
    [
        {
            "label": "API Debug Up",
            "type": "shell",
            "command":"docker compose -f ${workspaceFolder}/docker-compose.yml up --detach --build --wait",
            "dependsOn": [ "API Debug Down"]
        },
        {
            "label": "API Debug Down",
            "type": "shell",
            "command":"docker compose -f ${workspaceFolder}/docker-compose.yml down",
        }
    ]

}
